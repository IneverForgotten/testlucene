<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="com.baizhi.dao.Shopping2Dao">
    <select id="qeuryIndex" resultType="shopping">
        select * from shopping
    </select>
    <insert id="insertDB" >

        insert into shopping values(0,#{name},#{price},#{src},#{status},#{address},#{addTime},#{descripe})
    </insert>
</mapper>
